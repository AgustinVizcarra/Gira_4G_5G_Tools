apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ueransim-gnb.fullname" . }}-test-connection"
  labels:
    {{- include "ueransim-gnb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ueransim-gnb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
